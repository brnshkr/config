name: Validate Inputs

on:
  push:
    branches:
      - master
      - release/*

  pull_request:
    branches:
      - master
      - release/*

jobs:
  inputs:
    name: Validate Inputs
    runs-on: ubuntu-latest
    steps:
      - run: echo "temp"
