include ../../../../../conf/Makefile

#-- extras

#!! Makefile of a project that adds a fixer, an analyzer, a test and a grouping target of its own

CHECK_TARGETS += own-analyzer
FIX_TARGETS   += own-fixer
GROUP_TARGETS += own-group
TEST_TARGETS  += own-test

own-analyzer: #~~ this project's own analyzer
	@$(PRINTF) 'own analyzer\n'

own-fixer: #~~ this project's own fixer
	@$(PRINTF) 'own fixer\n'

own-group: #~~ this project's own grouping target
	@$(PRINTF) 'own group\n'

own-test: #~~ this project's own test
	@$(PRINTF) 'own test\n'
